import { useState } from "react"
// wpm = (characters typed - errors) / (5 * time in mins)

const [isCharCount, setIsCharCount] = useState(0)
const Wpm = () => {
  return (
    <div>Wpm</div>
  )
}

export default Wpm